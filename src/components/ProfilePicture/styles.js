import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 5,
    borderRadius: 38,
    borderWidth: 3,
    borderColor: '#ae22e0',
  },
  image: {
    borderRadius: 35,
    borderWidth: 1,
    borderColor: '#ffffff',
  },
});

export default styles;
